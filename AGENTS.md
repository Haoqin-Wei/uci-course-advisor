# 工程约定

## 学期规则

涉及学期计算、时间表达解析、会话查询目标、开课历史、比较回答、相关提示词或前端默认学期标签时，先阅读 [docs/term-rules.md](docs/term-rules.md)。该文档保存用户于 2026-09-20 确认的六条规则、实现位置及已知缺口。

- 保持 `current_term`、`default_term`、`query_terms`、`recent_query_focus`、`historical_reference` 的职责分离。
- 默认学期由美西时间和 UCI 校历计算，在第八教学周周一 00:00 切换；Spring 直接到 Fall。
- 前端默认标签只读；用户查询、比较和追问不能修改它。
- 优先处理本轮明确时间，再使用相关对话，最后使用默认值。未知数据不能当作未开课；历史教授不能当作未来教授。
- 不把现有实现缺口当成业务规则。修复缺口时加入对应反例回归并更新文档状态；用户确认新的规则时同步文档、实现和测试。
- 按文档的相关回归命令验证行为变化。文档核对通过与真实模型输出保证应明确区分。
